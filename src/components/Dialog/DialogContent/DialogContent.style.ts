import { createStyles } from "@/theme/core/tss/createStyles";

export default createStyles((theme) => {
  return {
    root: {
      flex: "1 1 auto",
      padding: "16px 24px",
      overflowY: "scroll",
    },
  };
});
